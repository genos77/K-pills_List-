data.json
data.json.tmp
.DS_Store
Thumbs.db
# NOTE: config.js contains your Supabase anon key.
# The anon key is safe to expose publicly (it's protected by RLS),
# so we DO commit it. If you ever switch to a service key, do not commit it.
